{
  "name": "lichess-practice",
  "version": "1.0.0",
  "description": "Objective: To practice positions in a repertoire.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "build": "esbuild src/script.ts --bundle --outdir=dist",
    "build:watch": "esbuild src/script.ts --bundle --outdir=dist --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chess.js": "^1.0.0-beta.6",
    "chessground": "^9.0.2",
    "chessops": "^0.12.7",
    "esbuild": "^0.19.2",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}
